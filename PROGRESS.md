# Progress

- Added `flowDiagram` / `flow` block in `BlockRenderer.jsx` for centered arrow/step diagrams (replaces misused `jsonTree` on retry article).
- Documented block in `BLOG_CONTENT.md`.
- Expanded `retry-storm-propogation.json` (sections 3–9); fixed invalid `publishedAt` line (`image.png` typo).
